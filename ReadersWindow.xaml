<Window x:Class="LibraryManagementSystem.ReadersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Readers Management" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Лейбл -->
        <Label Content="Фільтр відбору читачів:"
               Grid.Row="1"
               FontSize="14"
               FontWeight="Bold"
               Margin="0,0,0,5"
               VerticalAlignment="Center"
               HorizontalAlignment="Left" />

        <!-- Поле пошуку -->
        <TextBox Name="SearchBox"
                 Grid.Row="1"
                 Width="300"
                 Height="30"
                 Margin="0,0,0,10"
                 VerticalAlignment="Top"
                 FontSize="14"
                 TextChanged="SearchBox_TextChanged"
                 GotFocus="SearchBox_GotFocus"
                 LostFocus="SearchBox_LostFocus"
                 Tag="Search by Name, Class, Phone, Email"
                 Foreground="Gray" />

        <!-- DataGrid -->
        <DataGrid Name="ReadersDataGrid"
                  Grid.Row="2"
                  Margin="0,10,0,10"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,10,0,0">
            <Button Content="Додати / Add" Click="AddReader" Width="120" Margin="5" />
            <Button Content="Редагувати / Edit" Click="EditReader" Width="120" Margin="5" />
            <Button Content="Видалити / Delete" Click="DeleteReader" Width="120" Margin="5" />
        </StackPanel>
    </Grid>
</Window>
